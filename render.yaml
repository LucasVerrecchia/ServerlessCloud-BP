services:
  - type: web
    plan: free
    name: esgi-heroku-nodejs
    env: node
    buildCommand: npm install
    startCommand: node ./bin/www
    envVars:
      - key: DATABASE_URL
        fromDatabase:
          name: bpdb
          property: connectionString

databases:
  - name: bpdb
    plan: free
